<!--タグリスト-->
  <% @tag_list.each do |list|%>
    <i class="fa-sharp fa-solid fa-tag"></i>
    <%=link_to list.name,search_tag_path(book_tag_id: list.id) %>
    <%="(#{list.post_books.count})" %>
  <% end %>